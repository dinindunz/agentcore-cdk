[project]
name = "agentcore-mcp-calculator"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]==1.26.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = []
